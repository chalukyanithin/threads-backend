export const typeDefs = `#graphql
        type User {
            id: ID!
            firstName: String!
            lastname: String
            email: String!
            profileImageURL: String
        }
`